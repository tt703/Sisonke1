<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TlotloTau"
        tools:replace="android:theme">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Create Estimate Activity -->
        <activity android:name=".CreateEstimateActivity"/>
        <!-- Estimate Preview Activity -->
        <activity android:name=".EstimatePreviewActivity"/>
        <!-- Create Invoice Activity -->
        <activity android:name=".CreateInvoiceActivity" />
        <!-- Edit Company Info Activity -->
        <activity android:name=".EditCompanyInfoActivity" />
        <!-- Invoice Preview Activity -->
        <activity android:name=".InvoicePreviewActivity"/>
        <!-- Add Product Activity -->
        <activity android:name=".AddProductActivity"/>
        <!-- Sell Product Activity -->
        <activity android:name=".SellProductActivity"/>
        <!-- Edit Product Activity -->
        <activity android:name=".EditProductActivity"/>
        <!--Product List Activity -->
        <activity android:name=".ProductListActivity"/>
        <!--Manage Products Activity -->
        <activity android:name=".ManageProductsActivity"/>
        <!--Documents Activity -->
        <activity android:name=".DocumentsActivity"/>
        <!--Document Detail Activity -->
        <activity android:name=".DocumentDetailActivity"/>

        <!-- FileProvider for Sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>